# 操作手册 · EVE Online BR 战斗统计系统（通用版）

> 版本：v1.1 通用版 ｜ 适用对象：部署者 / 使用者 / 二次开发者
> 本文档同时充当「需自行修改清单」，第 6 章列出了源码中所有需要你按自己环境调整的位置。

---

## 目录

1. [这份文档解决什么问题](#1-这份文档解决什么问题)
2. [环境要求](#2-环境要求)
3. [安装与启动](#3-安装与启动)
4. [首次使用必做三步](#4-首次使用必做三步)
5. [功能使用说明](#5-功能使用说明)
6. [需自行修改清单（重点）](#6-需自行修改清单重点)
7. [配置项详解（config.py）](#7-配置项详解configpy)
8. [数据文件说明](#8-数据文件说明)
9. [打包为 EXE 发布](#9-打包为-exe-发布)
10. [上传到 GitHub](#10-上传到-github)
11. [常见问题 FAQ](#11-常见问题-faq)
12. [通用版相对原定制版改了什么](#12-通用版相对原定制版改了什么)

---

## 1. 这份文档解决什么问题

原程序是给某个特定军团定制的：军团名、军团头像、成员名单、战斗记录都硬编码在项目里。
现在的 **通用版** 已经把这些内容全部清除，改成了「谁部署、谁配置」的形式。

你需要做的只有两件事：

1. 按第 6 章的清单，改掉少数几个配置项；
2. 启动程序，在网页上添加**你自己的军团 ID**。

---

## 2. 环境要求

| 项目 | 要求 |
| --- | --- |
| 操作系统 | Windows 10 / 11（Linux、macOS 同样可运行，打包 EXE 仅限 Windows） |
| Python | 3.9 及以上（推荐 3.10 ~ 3.12） |
| 网络 | 能访问 `br.evetools.org`、`kb.evetools.org`。**建议关闭 VPN/代理**，否则接口易超时 |
| 浏览器 | Chrome / Edge / Firefox 等现代浏览器 |

---

## 3. 安装与启动

### 3.1 源码方式（推荐，方便改配置）

```bash
# 进入项目目录
cd br-web

# 创建并启用虚拟环境（可选但推荐）
python -m venv .venv
.venv\Scripts\activate          # Windows
# source .venv/bin/activate     # Linux / macOS

# 安装依赖
pip install -r requirements.txt

# 启动
python app.py
```

启动后会自动打开浏览器，默认地址 <http://127.0.0.1:5000>。
终端窗口关闭即停止服务。

### 3.2 EXE 方式（给不会 Python 的队友用）

1. 在自己的机器上执行 `python build.py`（需先 `pip install pyinstaller`）；
2. 把 `dist/BRStatisticsTool.exe` 连同 `data/` 目录一起发给队友；
3. 双击 EXE 即可，数据写在 EXE 同级的 `data/` 目录里。

> 首次双击 EXE 时，Windows 可能提示「已保护你的电脑」，点「更多信息 → 仍要运行」即可。

---

## 4. 首次使用必做三步

| 步骤 | 操作 | 说明 |
| --- | --- | --- |
| ① | 打开 **管理 → 军团管理** | 添加你要统计的军团 |
| ② | 填写「军团ID」和「军团名称」 | ID 是纯数字，例如 `98769610` |
| ③ | 回到 **战斗统计分析** 页面提交 BR 链接 | 未配置军团时会明确提示，不会生成空报表 |

**军团 ID 怎么找？**
打开 <https://zkillboard.com> 搜索你的军团，进入军团主页后看地址栏，
形如 `https://zkillboard.com/corporation/98769610/` —— 中间那串数字就是军团 ID。

### 4.1 出勤统计入口特别说明（重要）

程序采用 Flask 蓝图（Blueprint）组织路由，**出勤统计模块挂在 `/attendance` 前缀下**，
其内部页面的路径为：

```
出勤统计页面     http://127.0.0.1:5000/attendance/attendance
```

如果你在浏览器里直接访问 `/attendance`，**不会**看到出勤统计页面——
该地址返回的是 404。请通过以下任一方式进入：

- 点击首页顶部导航栏的 **「出勤统计分析」**（推荐，程序已内置正确链接）；
- 或手动访问完整地址 `/attendance/attendance`。

这是通用版保留的原程序结构，属于 Flask 蓝图 URL 前缀叠加，**不是故障**。

---

## 5. 功能使用说明

### 5.1 战斗统计分析（首页）

1. **选择报告日期**：报表文件名的日期部分，格式 `YYYYMMDD`；
2. **自定义报告名称**：报表文件名后缀，最多 50 字，非法字符会自动替换为 `_`；
3. **粘贴 BR 链接**：每行一个，格式必须是 `https://br.evetools.org/br/xxxxxxxx`，
   输入框下方会**实时校验**并提示有效 / 无效条数；
4. 点击 **「生成报告」**，进度条走完后页面自动刷新；
5. 点击 **「下载最新报告」** 获取 Excel。

生成的 Excel 包含：

| 列 | 含义 |
| --- | --- |
| 排名 | 按总伤害降序 |
| 角色 | 角色名 |
| 出击次数 | 该角色出现在战斗记录中的次数 |
| 击杀数 | 致命一击（final blow）次数 |
| 总伤害 | 累计输出伤害 |
| 使用舰船 | 期间使用过的舰船（去重，顿号分隔） |

右侧另附 **「总伤害排名前10」** 和 **「击杀数排名前10」** 两张柱状图。

> 统计范围：**只收录 `军团管理` 中列出军团的成员**，其他玩家会被自动忽略。

### 5.2 出勤统计分析

1. 选择 **报告日期** 与 **报告类型**（下拉选项来自 `config.py` 的 `EVENT_TYPES`）；
2. 可选填 **舰队说明**；
3. 在 **角色列表** 中粘贴本次出队的成员，**每行一个角色名**；
4. 点 **「添加舰队角色」**，右侧实时表格立刻刷新出勤次数；
5. 全部录完后点 **「导出Excel」**，得到含「出勤统计」+「历史记录」两个工作表，
   每行多次出现的角色会被合并累加。

> 出勤数据保存在 `data/attendance.json`，**重启程序不丢失**；
> 同一份名单里重复出现的角色会自动去重，不会重复计数。

### 5.3 管理页面

| 菜单项 | 作用 | 对应数据文件 |
| --- | --- | --- |
| 军团管理 | 维护要统计的军团 ID / 名称 | `data/corp_id.txt` |
| 角色管理 | 补充或修正角色 ID / 名称映射 | `data/char_id.txt` |
| 历史报告 | 查看 / 下载 / 删除历史报表 | `data/reports/` |
| 系统日志 | 排查报错 | `data/system.log` |

> **角色管理通常不用手动维护**。战斗统计过程中，程序会自动把 BR 里出现的本军团成员
> 补进 `char_id.txt`。只有当报表里显示 `Unknown(数字ID)` 时，才需要手工补一条。

---

## 6. 需自行修改清单（重点）

通用版已在源码中留好标记，**全项目搜索 `TODO(通用版)` 或 `【需修改】` 即可逐条定位**。

```bash
# 在项目根目录执行，列出全部待修改位置
grep -rn "TODO(通用版)\|【需修改】" --include="*.py" --include="*.html" --include="*.txt" .
```

### 6.1 必须修改（影响功能正确性）

| 优先级 | 文件 | 位置 | 要做什么 |
| --- | --- | --- | --- |
| 🔴 必改 | 网页「军团管理」 | — | 添加你自己军团的 ID 与名称，否则无法统计 |
| 🔴 必改 | `config.py` | 第 24 / 30 行 | 改掉系统名称与页脚文案为你的名字 |
| 🔴 必改 | `config.py` | 第 55 行 | `SECRET_KEY` 改成随机字符串（部署到公网时必改） |
| 🟡 建议 | `config.py` | 第 34 行 | 换成你自己的 LOGO 文件名，或填 `""` 隐藏 LOGO |
| 🟡 建议 | `config.py` | 第 46 / 49 行 | 监听地址与端口（局域网共用改成 `0.0.0.0`） |
| 🟡 建议 | `config.py` | 第 125 行 | `EVENT_TYPES` 改成你军团实际的活动类型 |
| 🟡 建议 | `version_info.txt` | 第 4 行起 | EXE 属性里的公司名 / 版权换成你自己的 |
| 🟡 建议 | `build.py` | 第 26 / 29 / 33 行 | EXE 名称、图标文件名、应用名 |

### 6.2 可选修改（按需调整）

| 文件 | 位置 | 说明 |
| --- | --- | --- |
| `config.py` | 第 64 行 | `DATA_FOLDER` 自定义数据目录（留空用默认） |
| `config.py` | 第 94 / 97 / 100 行 | 数据源接口地址，官方接口变更时更新 |
| `config.py` | 第 103 行 | BR 链接校验正则，**前后端共用**，改这里两边同时生效 |
| `config.py` | 第 106 行 | 请求超时秒数，网络慢时调大 |
| `config.py` | 第 114 行 | 报表文件名前缀 |
| `config.py` | 第 117 行 | 图表展示的前 N 名 |
| `attendance.py` | 顶部 `STORE_FILENAME` | 出勤数据文件名 |
| `templates/base.html` | 第 13 行附近 | CDN 地址；内网环境可换成本地 `static/` 文件 |
| `templates/attendance.html` | 第 24 行 | 出勤类型下拉框（已由 `config.EVENT_TYPES` 驱动） |
| `tools/make_icon.py` | 第 22 行 | 图标配色，重新执行脚本即可生成新图标 |
| `app.py` | 第 474 行 | 角色管理扩展点（如白名单功能） |
| `app.py` | 第 316 行 | 未配置军团时的提示文案 |

### 6.3 替换 LOGO / 背景图 / 图标的正确做法

| 想换什么 | 操作 |
| --- | --- |
| 导航栏 LOGO | 把图片放进 `static/`，然后改 `config.py` 第 34 行的 `CORP_LOGO` 为文件名 |
| 页面背景图 | 把图片放进 `static/`，然后改 `config.py` 第 38 行的 `BACKGROUND_IMAGE`；留空则用内置深空渐变 |
| 浏览器标签图标 + EXE 图标 | 改 `tools/make_icon.py` 配色后执行 `python tools/make_icon.py`；或直接把 `static/favicon.ico`、`app_icon.ico` 换成自己的 `.ico` |

> ⚠️ 背景图与 LOGO 请使用**你有权使用的图片**，不要直接搬运游戏官方美术素材。

---

## 7. 配置项详解（config.py）

| 变量 | 默认值 | 说明 |
| --- | --- | --- |
| `SITE_NAME` | `EVE Online BR 战斗统计系统` | 标题栏 / 导航栏 / 页脚统一使用 |
| `SITE_VERSION` | `1.1` | 页面显示为「系统名 v1.1」 |
| `FOOTER_TEXT` | 同 `SITE_NAME` | 页脚文案 |
| `CORP_LOGO` | `site_logo.svg` | 导航栏 LOGO，填 `""` 则不显示 |
| `BACKGROUND_IMAGE` | `""` | 背景图，留空用内置渐变 |
| `HOST` | `127.0.0.1` | 监听地址 |
| `PORT` | `5000` | 监听端口，被占用时改 5001 / 8000 |
| `AUTO_OPEN_BROWSER` | `True` | 启动后是否自动开浏览器 |
| `SECRET_KEY` | 占位值 | Flask 会话密钥，**公网部署必须改** |
| `DATA_FOLDER` | `""` | 自定义数据目录，留空用默认 |
| `BR_API_BASE` | evetools BR 接口 | BR 详情接口模板 |
| `KM_API_BASE` | evetools KM 接口 | 击杀邮件接口模板 |
| `ESI_BASE` | ESI 根地址 | 供 `ship_id.py` 使用 |
| `BR_LINK_PATTERN` | 正则 | 前后端共用的链接校验规则 |
| `REQUEST_TIMEOUT` | `10` | 请求超时（秒） |
| `REPORT_FILE_PREFIX` | `战斗报告` | 报表文件名前缀 |
| `CHART_TOP_N` | `10` | 图表前 N 名 |
| `EVENT_TYPES` | 4 项 | 出勤「报告类型」下拉选项 |

环境变量覆盖：设置 `BR_DATA_DIR` 可强制指定数据目录（优先级最高）。

---

## 8. 数据文件说明

全部位于数据目录（默认 `data/`），格式与用途详见 `data/README.md`。

| 文件 | 是否入库 | 说明 |
| --- | --- | --- |
| `ship_id.txt` | ✅ 已预置 | 舰船 ID→名称映射（公共数据，6000+ 条） |
| `corp_id.txt` | ❌ 本地生成 | 军团配置，通过网页添加后自动创建 |
| `char_id.txt` | ❌ 本地生成 | 角色映射，统计时自动写入 |
| `br_links.txt` | ❌ 本地生成 | 上次提交的链接，用于页面回显 |
| `attendance.json` | ❌ 本地生成 | 出勤记录 |
| `reports/*.xlsx` | ❌ 本地生成 | 生成的报表 |
| `*.log` | ❌ 本地生成 | 运行日志 |

**格式约定**：UTF-8 编码，每行 `ID_名称`，以**第一个下划线**分隔；
空行和 `#` 开头的注释行会被忽略。

**舰船名显示 `Unknown(数字ID)` 怎么办？**
说明有新船没收录，执行 `python ship_id.py` 重新拉取舰船数据即可。

---

## 9. 打包为 EXE 发布

```bash
pip install pyinstaller pillow
python tools/make_icon.py     # 可选：重新生成图标
python build.py               # 产物：dist/BRStatisticsTool.exe
```

打包后分发时，请把 **EXE 文件 + `data/` 目录** 一起拷贝（`data/` 可只带 `ship_id.txt`）。

---

## 10. 上传到 GitHub

项目已内置 `.gitignore`（自动排除 `build/`、`dist/`、日志、用户数据、密钥）。

```bash
git init
git add .
git commit -m "feat: 通用版 BR 战斗统计系统"
git branch -M main
git remote add origin https://github.com/<你的用户名>/<仓库名>.git
git push -u origin main
```

> ⚠️ **不要把 GitHub Token 或 `SECRET_KEY` 写进代码或提交进仓库。**
> 如果 Token 曾经出现在聊天记录、日志或截图里，请立刻到 GitHub
> `Settings → Developer settings → Personal access tokens` 中吊销并重新生成。
>
> 建议改用更安全的方式推送：
> ```bash
> # 方式一：Git 凭据管理器（首次输入后自动记住）
> git push -u origin main
>
> # 方式二：临时令牌（注意不要留在命令行历史/脚本里）
> git remote set-url origin https://<用户名>:<Token>@github.com/<用户名>/<仓库名>.git
> ```

---

## 11. 常见问题 FAQ

**Q1：点「出勤统计分析」后地址是 `/attendance`，页面 404？**
蓝图路由实际为 `/attendance/attendance`。请从导航栏点击进入，或手动访问
`http://127.0.0.1:5000/attendance/attendance`。详见第 4.1 节。

**Q2：点「生成报告」后提示「尚未配置任何军团 ID」？**
通用版默认军团列表为空，请先到 **管理 → 军团管理** 添加军团。

**Q3：报表里角色显示 `Unknown(2118200607)`？**
该角色的 ID 与名称映射缺失。程序下次统计时会自动补全；若仍未补全，可到
**角色管理** 手动添加 `ID_名称`。

**Q4：报表里舰船显示 `Unknown(12345)`？**
新版本加入了新舰船，执行 `python ship_id.py` 更新 `data/ship_id.txt`。

**Q5：提示 `Invalid links` / `Invalid BR link`？**
链接格式必须严格是 `https://br.evetools.org/br/<ID>`，不能带多余参数；
也可能是 BR 已过期或接口临时不可用，可刷新重试。

**Q6：请求一直超时？**
关闭 VPN / 代理；或在 `config.py` 把 `REQUEST_TIMEOUT` 调到 20~30。

**Q7：端口 5000 被占用？**
改 `config.py` 的 `PORT`，或 Windows 下执行 `netstat -ano | findstr :5000` 找到占用进程。

**Q8：换了电脑，历史报表和出勤数据怎么迁移？**
整个 `data/` 目录拷贝到新机器的同级位置即可。也可用环境变量 `BR_DATA_DIR`
把数据目录指到网盘同步目录。

**Q9：出勤数据存在哪？会丢吗？**
存在 `data/attendance.json`，重启不丢。备份该文件即可。

**Q10：局域网内其他人怎么访问？**
把 `HOST` 改成 `"0.0.0.0"`，其他人用 `http://<你的内网IP>:5000` 访问；
注意 Windows 防火墙需放行该端口。**公网暴露前务必修改 `SECRET_KEY`。**

---

## 12. 通用版相对原定制版改了什么

为便于对照，本次改造的完整变更如下：

### 12.1 已清除的内容

| 项目 | 处理方式 |
| --- | --- |
| 军团头像 `static/corp_logo.png` | **已删除**，改为中性的 `static/site_logo.svg` |
| 军团专属图标 `cosmic_wanderers_icon.ico` | **已删除**，改为通用的 `app_icon.ico` / `static/favicon.ico` |
| 军团背景图 `static/background.jpg` | **已删除**（2.3MB 的官方美术素材），改为内置 CSS 深空渐变背景 |
| 军团名 `Cosmic-Wanderers` | 全部替换为 `config.SITE_NAME` 可配置项（标题、导航、页脚、文档页） |
| 军团 ID `98769610` | `data/corp_id.txt` **已清空** |
| 260 条真实角色 ID / 名称 | `data/char_id.txt` **已清空** |
| 3 条真实 BR 链接 | `data/br_links.txt` **已清空** |
| 运行日志中的战斗与报表记录 | `data/system.log`、`data/detailed.log` **已删除** |
| 作者署名『蔷薇』『76』 | 文档页中**已移除** |
| 遗留产物 `output.xlsx`、`__pycache__/` | **已删除** |

> 经全项目检索，`Cosmic-Wanderers`、`蔷薇`、`76` 等特定军团 / 作者标识已无残留。

### 12.2 顺手修复 / 增强的问题

| # | 问题 | 修复方式 |
| --- | --- | --- |
| 1 | 出勤数据只存内存，**重启即丢** | 改为持久化到 `data/attendance.json` |
| 2 | 导出 Excel 会在工作目录留下 `output.xlsx` 垃圾文件 | 改为内存流输出（`BytesIO`），不落盘 |
| 3 | 下载 / 删除报告接口存在**目录穿越漏洞**（`../../../` 可读写任意文件） | 增加白名单校验 `resolve_report_path()` |
| 4 | 出勤统计表格用 `innerHTML` 直接插入角色名，存在 **XSS 风险** | 增加 `escapeHtml()` 转义 |
| 5 | 打包成 EXE 后数据被写到临时目录，**重启即丢** | 数据目录改为「EXE 同级 / `data`」，并支持 `BR_DATA_DIR` 覆盖 |
| 6 | 文件读写未指定编码，中文在部分系统会乱码 | 统一显式 `encoding='utf-8'` |
| 7 | 前端 BR 校验正则与后端**各写一份**，易不一致 | 统一由 `config.BR_LINK_PATTERN` 注入（`tojson` 到 JS） |
| 8 | `manage_chars` / `manage_corps` / `history` / `logs` 四个页面**没有导航入口** | 导航栏新增「管理」下拉菜单 |
| 9 | 提交表单出错后已填内容丢失 | 回显日期、名称与 BR 链接，并自动保存上次链接 |
| 10 | Bootstrap 引入了两个版本（5.1.3 + 5.3.0）、Font Awesome 重复引入 | 清理为单一版本，并全部换用国内更稳定的 bootcdn |
| 11 | 未配置军团时生成**空白报表**，无从判断原因 | 改为明确报错提示引导到「军团管理」 |
| 12 | 出勤名单重复角色会重复计数 | 提交时按顺序去重 |
| 13 | 删除报告按钮的 `onclick` 拼接文件名，含引号会**报错** | 改用 `data-*` 属性 + `encodeURIComponent` |
| 14 | 角色 / 军团列表为空时页面一片空白 | 增加空状态提示与条目计数 |
| 15 | `ship_id.py`、`app.py` 中残留**个人绝对路径** | 改为基于 `config.get_data_folder()` 动态解析 |
| 16 | `docs.html` 存在嵌套 `<h3><h3>`、`<ol>` 未闭合等**标签错误** | 重写为规范结构 |

---

## 附：一分钟上手流程

```
1. pip install -r requirements.txt
2. 改 config.py 第 24 / 30 行的系统名称（可选）
3. python app.py
4. 浏览器 → 管理 → 军团管理 → 添加你的军团ID_名称
5. 首页 → 填日期、名称 → 粘贴 BR 链接 → 生成报告 → 下载
6. 需要出勤统计 → 导航栏「出勤统计分析」→ 录入名单 → 导出Excel
```
